## Setup Github 
### One time
```
git init
git checkout main
git branch -m main
git remote add origin https://repository_url
git pull --rebase
```

### Everytime
```
git status
git add .
git commit -m "some message"
git push
```