// WAP to print a string
#include<stdio.h>
int main() {
    char a;
    int size = sizeof(a);
    printf("Size of Character %d\n", size);

    int aInt;
    int sizeInt = sizeof(aInt);
    printf("Size of Integer %d\n", sizeInt); 

    return 0;
}